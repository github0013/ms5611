require "spec_helper"

RSpec.describe Ms5611 do
  it "has a version number" do
    expect(Ms5611::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
